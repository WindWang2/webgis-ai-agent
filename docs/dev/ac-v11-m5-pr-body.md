## M5 · 规模化验证与终版收口（W8 + W9，ADR-0168/0169）· **本线终点**

> 关联任务书:`goals-ac/V11-master-adaptive-cartography-1B.md`(§3 W8–W9/§6/§7)。
> 分支策略(§0.6):同分支叠加(M1–M4 合入后本 PR 即只含 W8+W9)。
> **按 §7 纪律,本 PR 不自行合并。** 本文件在前序 PR 合入后用于创建
> （分支终点 `ac-v11-w9`;M5 合入后清理 worktree 构建产物,保留 worktree
> 待人工处置 —— §7 收口纪律）。

### 1. 目标

- **W8(ADR-0168)**:C4 成本/波次扩展（迁移 0058）、408/1632 矩阵、波次
  ratchet（劣化 100% 拦截）、盲评计划、成本治理。
- **W9(ADR-0169)**:债清核准、契约升级测试、文档定稿、i18n/埋点/安全
  复核、终版门禁与台账。

### 2. 交付摘要

- **W8**:`scale_matrix.py`（core 408 组实跑 0.2s / full 1632;每组
  {qualityMetrics, costTokens, costMs, artifacts};成本诚实 0+llmUsed=false;
  预算告警;度量均值 golden 冻结）;迁移 0058（runs 增 wave/contract_version/
  map_type/cost_tokens/cost_ms、metrics 增 wave）;`Observation.wave` +
  `aggregate_observations_by_wave` + 矩阵观测桥 + 劣化注入 100% 拦截测试;
  盲评卡（预注册判据 + 60 组抽样计划,人工评分待执行）。
- **W9**:契约升级测试（C1–C4 v10→v11 共 6 例）;债清核准（label 适配层
  为生产 API;六「孤儿」经引用核验为工具/测试消费 → 重登记保留）;
  ADR-0160~0169 + W9 台账 + 全波汇总;安全/i18n/埋点复核（无高危项、
  可见文案零条如实、四类埋点映射既有结构）。

### 3. 数值对照（M4 → M5 终版）

| 指标 | M4 | **M5 终版** |
|---|---|---|
| cartography 覆盖率 | 57.7% | **~58% 区间**（floor 50,只升不降） |
| 验证矩阵 | 无 | **core 408 实跑 / full 1632** |
| C4 事实库 | wave/cost 无列 | **迁移 0058 增列** |
| ratchet | 图型×检查项 | **+波次维度;劣化 100% 拦截实证** |
| 契约升级 | 口头纪律 | **6 例机器断言** |
| 迁移总数（本线） | 0057 | **0057 + 0058** |

### 4. 门禁原文（§6;SKIP_BROWSER=1 全量一次跑通）

（见本 PR 评论区终版原文;覆盖/lane 测试量以实际输出为准。）

### 5. 全线收口声明与缺口登记

- **W0–W9 十波全交付**（ADR-0160~0169 十篇,tag `ac-v11-w0`…`ac-v11-w9`）。
- **如实缺口**（W9 台账 §全波汇总后登记）:像素 golden ≥120 浏览器批次、
  盲评人工评分、渲染器物理合并、四处接口接线、前端阈值同义词表 ——
  每项有落点（文件/批次）与判据,不以「已打勾」掩饰。
- **门禁纪律入档**:pytest 并发共用 `.coverage` 数据文件（W6 台账运维
  教训）——门禁运行期间禁止并发 pytest。

### 6. 风险与回滚

- 全面回滚 = 回 `origin/master`;按波回滚 = 各 tag;迁移 0057/0058
  downgrade 反序可回。
- M5 合入后动作（§7）:清理 worktree 构建产物（保留 worktree 待人工处置）。

### 7. 兼容性

- 本线全部契约为**只加不改**（v10 工件可读性由升级测试锁定）;
  迁移为只增列;前端依赖新增一个 MIT 运行时包;`.github/workflows/**`
  全程未触碰。
