engine-strict=true
# eslint-config-next@14.2.35 的 peer 声明仍为 eslint ^7||^8（Next 14 时期），
# 升级 ESLint 9 后会产生 peer 冲突。用 legacy-peer-deps 跳过 peer 校验，
# 使本地与 CI（npm ci）行为一致；锁文件已含完整解析树。
legacy-peer-deps=true
