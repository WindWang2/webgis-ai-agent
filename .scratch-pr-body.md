# feat(api): V9 contract foundation — response models, unified envelope, idempotency, v2 layer (ADR-0138)

## 目标

按 V9 任务书（goals-v9/01-foundation-api-contract-v9.md）根治对外契约层六类短板：
response_model 缺口、双错误信封、分页混杂、HTTP 无幂等、文档漂移、版本策略缺失。
交付 P0–P8 全部阶段 + ADR-0138 + 独立 CI contract lane。

## 复核纪要（§0.2 防重复）

- **PR 搜索**（contract OR response_model OR openapi OR idempotency OR pagination OR api-docs OR envelope）：
  唯一直接先例为 **#123**（已合并，范围仅 chat 会话分页 + 少量 response_model）——本线为其全量扩展，无范围重叠。
  其余命中均为各特性 epic（cartography/lakehouse/workflow 等）的自述提及，不构成契约层工作重叠。
- **Issue 搜索**：#1217（POST /upload TS/Python 契约漂移，已关闭）——master 抽查确认 TS 侧修复已落地
  （`frontend/lib/api/upload.ts` crs 可空 + additive 字段），但**字段级契约闸未建** → 本线 P8 交付（后端半边）。
  #901（ApiResponse 泛型缺陷，已关闭）——master 抽查确认 `ApiResponse(BaseModel, Generic[T])` 已落地，本线直接受益。
  #1216（quality_runner lane 映射 bug，已关闭）——master 抽查确认前缀匹配已修复，changed-lane 机制对本线改动生效。
- **分支**：无 contract/api-v/openapi 同类分支在途。
- **api-docs 现状确认**：lakehouse / geocompute / workflow-runtime 三章节缺失、限流 60/min（文档）vs 240/min（`app/main.py` 实际）漂移均在 —— 与任务书一致，P6 修复。
- **结论：(a) 无重叠 → 按任务书全量执行。** 已关闭 issue 修复未被回归（抽查通过）。

## 变更摘要

- **P0**：`scripts/api_contract_recon.py`（可重跑）从路由表 dump 全量矩阵 —— 实测 220 端点 / 123 无 response_model / 65 内联模型（任务书 221/163/69 为较早 master 口径，差异见勘察报告 §10）。产出 `docs/dev/api-contract-recon.md` + `api-contract-matrix.csv`（P0 基线）+ `api-contract-matrix-final.csv`（终态）。
- **P1**：65 个内联 BaseModel verbatim 迁入 `app/schemas/{auth,chat,config,explorer,geocompute,health,knowledge,map,mapspec_mutation,project,report,task,upload,workflow_runtime,workflow_resume}_schema.py` + templates/data_fabric/project 追加块；路由文件只 import（校验器、`_bounded_canvas`、`MAX_CONTENT_LENGTH` 等行为面原样保留，测试引用的模块属性以 `# noqa: F401` 保持）。
- **P2**：123 个缺口端点全部挂模；16 个流式/二进制/定制 media-type 端点进入 `EXCLUSIONS` 豁免清单（逐条理由，测试强制清单卫生）；204 端点显式表达无体。类型不准处先修实现再挂模型（例：`extensions_api` CORE_API_VERSION 实为字符串）。wire 形态保真：`response_model_exclude_none` 保持 map /export 与 map-action-ack 的历史「键缺席」语义。
- **P3**：HTTPException（**fastapi 与 starlette 两层**——模糊测试发现 starlette 层 body 解析 400 绕过全局 handler）与 RequestValidationError 接入统一 `{code,success,message,data}` 信封 + errors 分类学附加字段；503/502/504 不再误标 SERVER_ERROR。回滚面：`LEGACY_DETAIL_ENVELOPE` settings 开关 + `X-Error-Envelope: detail` 请求头。`frontend/lib/api/transport.ts` 双信封兼容（+5/-1 行，≤30 红线；typecheck 绿）。
- **P4**：`Page[T]` 契约保持不变（改字段集破坏 #123 既有采用方）；additive 补齐 chat/sessions `has_more`；clamp 边界 + 端点级边界测试。ad-hoc limit alias 的 OpenAPI deprecated 标注**推迟到 v2**（v1 无替换信封即弃用是虚假弃用；§8「接口不 breaking」优先）——ADR-0138 D3 记录。
- **P5**：`IdempotencyMiddleware`：仅对带 `Idempotency-Key` 的 JSON POST 生效（无头零开销）；key=hash(header+method+path+body)；TTL 24h；SET NX 单飞（60s 锁过期退化为有界重复）；fail-open；重放含状态码与可安全复制的响应头。四类门禁测试全绿（重放一致/并发单飞/TTL/fail-open）。
- **P6**：`scripts/gen_api_docs.py`（marker 区间生成，手写导言保留，运行时常数从代码读取）+ `tests/test_api_docs_drift.py`（逐字比对 + 限流数值闸 + 三子系统章节在场闸）；限流 60 vs 240 漂移修复。
- **P7**：`/api/v2` 挂载（同一 router 复用，lakehouse/geocompute/workflow-runtime 三示范子系统 59 路径，operation_id `v2_*` 唯一）；v1 全量 `Deprecation`/`Sunset`/`Link successor-version` 头 + `webgis_api_version_requests_total` 打点；不下线任何 v1 端点。
- **P8**：Schemathesis 4.26 ASGI 内联模糊 shard（max_examples=30，allowlist 限定无外部依赖面）——发现并修复 3 类真实缺口（auth 401/403/429/400 状态未声明、503 误码、starlette 层绕过）；字段级契约闸（#1217 后端半边：响应字段签名快照，漂移 fail + 类型化 diff）；独立 `.github/workflows/contract.yml`（production.yml 零改动）。

## 交付台账

见 **docs/dev/api-contract-v9-ledger.md**（任务 → 文件 → 测试 → 证据 四列 + 基线失败甄别 + review 处置）。

## 测试证据

- 契约门禁：`tests/unit/api_contract` 58✓（覆盖率门禁/信封 9/幂等 10/分页 5/v2 5/字段闸 1/子系统参数化 19/schemathesis 6）
- 文档闸：`tests/test_api_docs_drift.py` 3✓
- 迁移期逐批回归：auth 32✓、health 19✓、templates 18✓、knowledge 7✓、geocompute 550✓、workflow_runtime 144✓、project 211✓、mapspec 58✓、chat 56+8+37✓、data_fabric 7✓、report 16✓、task 74✓、config 41✓、layer/descriptor 23✓ 等
- 全量套件（串行分片取证，Windows 本地）：2540+2647+138 通过；21 个失败全部为基线既有环境类（extensions_platform rlimit/bwrap/multiprocess 11 项、路径分隔符/sys.path 类 4 项、Node CLI 缺席 1 项、pytest 9.1.1 venv 与 test_coord_transform import-mock 交互 5 项——后四类与 #1011 甄别清单同类；逐条甄别见台账）。Linux CI 不受影响。
- 覆盖率 ≥75%：按 pytest.ini 注释由 CI production.yml 主 lane `--cov-fail-under=75` 执行。
- `ruff check <全部变更 .py 文件>`：0 告警。
- OpenAPI 快照：本次为 additive+refresh 变更，`API_SNAPSHOT_UPDATE=1` 刷新（test_api_compatibility 闸绿）。

## 风险与回滚

- **统一错误信封是 error 路径的破坏性变更**。回滚面 = `LEGACY_DETAIL_ENVELOPE=true`（部署级一行环境变量，无需回滚代码）；灰度可用请求头 `X-Error-Envelope: detail` 按 v1 客户端逐个覆盖。前端 transport.ts 已双信封兼容。
- response_model 挂模对返回体校验+重序列化：开放对象（extra=allow）不裁剪字段；严格模型处曾出现多余 null 键，已用 `response_model_exclude_none` 保形。
- 幂等层任何故障 fail-open，不影响可用性；v2 拒绝 legacy 降级（语义承诺，有测试）。

## 与其他 v9 线协调点

- 路由文件仅改签名/schema/response_model 行区，B 线 `Depends(...)` 行未触碰；合并顺序 A 先 B 后，B 负责 rebase。
- D/E/F/H 前端 API 客户端以本线 OpenAPI 生成物为准：接口不 breaking（additive 字段 + exclude_none 保形 + LEGACY 开关兜底 + transport.ts 兼容已交付）。TS 类型再生成（#1217 前端半边）受 §8 前端边界约束未动手，字段闸已就位可独立跟进。
- 迁移编号：未新建 Alembic 迁移（0036–0055 保留段未占用）；ADR 编号实测 watermark 0137 → 本线占用 **0138**（与任务书预期一致）。
